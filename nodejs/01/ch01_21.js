function add2(x, y) {
    return x + y;
}

console.log(add2(1, 2));

const add1 = function(x, y) {
    return x + y
}

console.log(add1(1, 2));

// arrow 함수 
let add3 = (x, y) => {
    return x + y;
}

console.log(add3(1, 3));